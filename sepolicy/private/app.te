# Allow appdomain to get vendor_persist_camera_prop
get_prop(appdomain, vendor_persist_camera_prop)

# allow prip-app , platform_app denials
allow priv_app default_android_service:service_manager find;

allow platform_app default_android_service:service_manager find;
